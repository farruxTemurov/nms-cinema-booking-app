# App Info
spring.application.name=movie-booking-app
server.port=9090

# Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/movie_booking_db?useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=Farrux2006

# Hibernate/JPA Config
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# JWT Config (We\u2019ll use this later)
app.jwt.secret=your_jwt_secret_key_here
app.jwt.expiration=3600000

# CORS Setup (optional, needed for Angular)
spring.mvc.cors.allowed-origins=http://localhost:4200
spring.mvc.cors.allowed-methods=GET,POST,PUT,DELETE
